# 对冲比例修改意见

## 1. 背景

当前数据口径中，空头市值使用了负号表达（例如 `-1,659,520.08`）。  
如果直接用带符号空头市值计算“对冲比例”，会出现负百分比，容易与业务语义冲突。

建议将指标拆分为两个：

- **对冲比例**：衡量“空头绝对规模 / 多头规模”，通常展示为正值百分比。
- **净敞口比例**：衡量组合方向暴露，保留正负方向（正=净多，负=净空）。

---

## 2. 统一口径（建议）

设：

- `L` = 多头市值（应为正数）
- `S` = 空头市值（当前存储为负数）
- `T` = 目标对冲比例（例如 `100%`）

先做标准化：

- `S_abs = abs(S)`

### 2.1 对冲比例（建议展示字段）

```text
hedge_ratio = S_abs / L * 100%
```

解释：

- `100%`：空头绝对市值与多头市值相等
- `>100%`：超对冲
- `<100%`：欠对冲

### 2.2 对冲偏差

```text
hedge_deviation = hedge_ratio - T
```

解释：

- `>0`：超对冲（高于目标）
- `<0`：欠对冲（低于目标）

### 2.3 净敞口比例（新增或替代原“带符号对冲比例”）

```text
net_exposure_ratio = (L - S_abs) / L * 100%
```

解释：

- `>0`：净多
- `<0`：净空
- `=0`：完全对冲

---

## 3. 展示命名建议

- 原“对冲比例”字段：使用 **`对冲比例`**（按绝对值口径）
- 原“对冲偏差”字段：保持 **`对冲偏差`**（对冲比例 - 目标）
- 新增字段：**`净敞口比例`**（正=净多，负=净空）

---

## 4. 色彩规则建议

### 对冲偏差

- `abs(偏差) <= 5%`：绿色（可接受区间）
- `5% < abs(偏差) <= 15%`：橙色（关注）
- `abs(偏差) > 15%`：红色（告警）

### 净敞口比例

- `>0`：红色（净多）
- `<0`：绿色（净空）
- `=0`：灰色（中性）

---

## 5. 计算示例

假设：

- 多头市值 `L = 15,504,779.31`
- 空头市值（存储）`S = -1,659,520.08`
- 目标对冲比例 `T = 100%`

则：

- `S_abs = 1,659,520.08`
- 对冲比例 `= 1,659,520.08 / 15,504,779.31 = 10.70%`
- 对冲偏差 `= 10.70% - 100% = -89.30%`
- 净敞口比例 `= (15,504,779.31 - 1,659,520.08) / 15,504,779.31 = +89.30%`（净多）

---

## 6. 实施注意事项

1. **不要在“对冲比例”中直接使用带符号空头市值**；应先取绝对值。  
2. 当 `L <= 0` 时，相关比例应返回 `N/A`，避免除零或误导。  
3. 页面和导出报表要同步字段定义，避免“页面是绝对值、导出是带符号”导致歧义。  
4. 历史数据若需回溯对比，应标注“口径切换日期”。

