# 数据页面流程说明

业务数据落在 MongoDB。多数页面是读库展示；少数打开时再算。定时任务一般只在交易日跑，净值类业务日多为 T-1。

---

## 一、数据怎么进系统

```
邮件 / FTP / 外部系统 / 本地文件
        ↓
   定时解析入库（MongoDB）
        ↓
   页面读库展示（少数现场计算）
```

| 途径 | 做什么 |
|------|--------|
| 邮件（IMAP） | 收托管/管理人/券商等附件，解析入库 |
| FTP | 拉持仓 CSV、T0 日内汇总等入库 |
| 外部系统 | QMT 交易日志由仓外策略写入（本系统只摘录）；米筐更新基准；Wind 等打开页按需查 |
| 本地文件 | 历史净值、波动率、交易日历等按需导入 |

---

## 二、一日主流程

1. **清晨** — 清日度持仓分析缓存；部分对账单；米筐基准；部分净值  
2. **午前** — Alpha 日报与源/目标持仓；继续错开导入各产品净值  
3. **午后** — 继续净值、持仓对账单等  
4. **15:45** — 从交易日志摘录证券账户收盘快照 → 账户简报 / 日度持仓分析底座就绪  
5. **傍晚～晚间** — 更多净值、期货结算、T0 日内 FTP；每周首个交易日再拉 T0 周度邮件  

成败看「定时任务运行日志」，可按日志重跑。

---

## 三、各页面流程

### 基金净值
托管/管理人净值邮件（可补本地 Excel）→ 真实净值库（按产品分集合）→ 读库出图出表

### alpha 产品表现
Alpha 日报邮件净值 + 米筐基准 → 打开页对标计算（可写回结果）→ 对比图与表

### 净值曲线（菜单可能不展示）
Alpha 净值库 → 多选产品读库画曲线（不算对标）

### Alpha 产品日报（菜单可能不展示）
Alpha 日报邮件 → 入库 → 按日读宽表

### T0 表现
- 汇祝：FTP「吾执日内交易汇总」→ 傍晚入库 → 日内汇总表  
- 深度秩序：周度绩效邮件 → 每周首个交易日晚间入库 → 周度绩效表  

### 账户简报
QMT 交易日志（期货账户读实时库）→ 15:45 取收盘窗口最新一条写入快照库 → 读快照拼简报表  

默认日：交易日 15:45 前看 T-1，之后看当日；非交易日看最近交易日。

### 日度持仓分析
收盘快照持仓 + Wind 等增强 → 打开页现场生成报告（可缓存）

### 波动率指标（菜单可能不展示）
本地 CSV 导入 → 波动率库 → 读库展示

### 定时任务运行日志
各定时任务结束写日志 → 查成败 / 按日志重跑

---

## 四、只入库、暂无业务页

| 数据 | 来源 | 时机概要 |
|------|------|----------|
| 基金持仓对账单 | fareport 等邮件 | 上午 / 午后，多为 T-1 |
| 证券资金 / 两融对账单 | 券商邮件 | 上午等 |
| 期货结算单 | 期货公司邮件 | 傍晚～晚间 |
| Alpha 源持仓 | 邮件压缩包 CSV | 午前 |
| Alpha 目标持仓 | FTP 按日 CSV | 午前（业务日多为当天） |
| 交易日历 | 本地导入 | 按需；全平台是否交易日依赖它 |
